import styles from "../styles/Navbar.module.css";

export const Navbar = () => {
    return (
        <nav className={styles.nav}>
            <h1 className={styles.icon}>Hey</h1>
        </nav>
    )
}